# Drift Report: Documentation vs. Runtime on Port 8011

This report captures the mismatches between stated documentation (README, service profiles, contract registry) and actual runtime exposure on the production port `8011`.

| Document | Claim | Actual Runtime Reality | Drift Category |
|---|---|---|---|
| `README.md` (lines 84-97) | `8011` exposes multiple execution and operator route families; the explicit `NT` execution contract is limited to `/health`, `/api/v1/live/*`, `/api/features/btc/*`, and `/api/signals/btc/*`. | Runtime mounts those execution families plus operator/documentation families (`charts`, `questdb`, `entities`, `whale`, `meta`, FastAPI docs routes). README is high-level aligned, but the exact non-execution exposure must be carried by the route inventory and boundary artifact. | Documentation Granularity |
| `docs/PRODUCTION_CONSUMER_SERVICE_PROFILE_2026-04-05.md` (lines 43-49) | The core live bundle on `:8011` includes `/api/v1/live/*`, `/api/v1/charts/*`, `/api/prices/*`, and `/api/metrics/latest` under a mixed `tier_1_execution` / `tier_2_operator` heading. | Runtime mounts the live router plus charts and QuestDB routers on `:8011`; the profile does not provide the stricter execution-versus-operator split needed by `spec-054`. | Tier Granularity |
| `docs/PRODUCTION_CONSUMER_SERVICE_PROFILE_2026-04-05.md` (lines 74-88) | BTC feature bundles and BTC signal snapshots are production consumer surfaces on `:8011`. | Runtime mounts `api.routes.features` and `api.routes.signals` on `:8011`; these claims are runtime-aligned and should be carried into the Phase 2 tiering baseline. | Service Profile Coverage |
| `docs/PRODUCTION_CONSUMER_SERVICE_PROFILE_2026-04-05.md` (lines 136-143, 180-185) | Wallet/cohort latest routes and whale/event routes belong to the production-ready consumer service on `:8011`. | Runtime mounts the QuestDB-backed cohort routes and whale router on `:8011`; `spec-054` still needs to classify whether they are execution inputs or operator/forensics surfaces. | Tier Granularity |
