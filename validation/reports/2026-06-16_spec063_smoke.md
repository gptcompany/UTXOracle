# spec-063 Smoke Report

**Date**: 2026-06-16
**Branch HEAD**: 56f8628 (post T021+T022 GREEN)
**Tester**: spec-063 implementation TDD cycle

## T013 — Manual smoke (Phase 3 US1)

**Status**: ⚠️ PARTIAL — code complete, end-to-end smoke against production data BLOCKED.

**Attempt** (by Gemini at HEAD f6ffd75): invoked `uv run python -m scripts.live.flow_aggregator` against `data/utxoracle.duckdb` (≈ 57 GB).

**Outcome**:
- `aggregate_flows()` killed with exit code 137 (OOM SIGKILL) during the `INSERT OR REPLACE INTO entity_flows_daily ... GROUP BY entity_id, date` against the production-scale `entity_transfer_edges` table.
- Immediately after, host QuestDB PG-wire connection returned "Connection reset by peer" — cascade from the parent process termination.

**Root cause analysis**:
- `data-model.md` R6 assumed cardinality ≤ 10 000 rows per run with memory ceiling ≈ 4 MB.
- Production `entity_transfer_edges` cardinality exceeds the assumption by orders of magnitude (no explicit measurement during discovery — gap noted in [decisions.md](../../specs/063-entity-flows-daily-questdb/decisions.md) D6 production runtime gap notice).
- The OOM is in the DuckDB legacy aggregation SQL, NOT in the spec-063 dual-write code path. spec-063 code never executed end-to-end.

**Conclusion**:
- Spec-063 dual-write **pattern validation**: unit-level (26/26 tests pass) + integration-level (T020 against live QuestDB) demonstrate that the dual-write contract is implemented correctly.
- Spec-063 **end-to-end production smoke**: deferred. Requires streaming / chunking of the DuckDB aggregation, OUT OF SCOPE per the hard constraints in `plan.md` Technical Context.
- Operator action: invoke `aggregate_flows()` on a smaller DuckDB fixture or wait for a follow-up streaming spec.

## T023 — Rollback runbook smoke (Phase 5 US3)

**Status**: ✅ PASS (unit-level integration test T020 covers the equivalent property)

**Verification**:
- T020 (integration-marked, ran against live host QuestDB at HEAD `ebf53b9`): seeded two sentinel rows with distinct floats `inflow_btc = 999.123456` and `888.654321`; ran `aggregate_flows()` with `SPEC063_QUESTDB_WRITE=0` against a temporary fixture DuckDB; both sentinel rows survived unchanged.
- T014 (unit, ran at every CI cycle): asserted that with `SPEC063_QUESTDB_WRITE=0`, `_open_pg_sync` is NOT called by the producer and the disabled-state INFO log line is emitted exactly once.

The rollback runbook (`quickstart.md` Rollback Step 1–3) is therefore validated at the unit + integration boundary. End-to-end production validation (systemd unit reload + journal grep) deferred to operator post-merge.

## Test sweep

```
uv run pytest tests/test_flow_aggregator_questdb.py -q
26 passed, 26 warnings in 9.28s
```

Including:
- 17 parameterised cases of `test_should_write_questdb_parser_table`
- T005 signature
- T010 byte-identity
- T011 cast contract
- T014 disabled-state log
- T015 failure isolation
- T018 webhook fires on failure
- T019 webhook NOT on success
- T020 (integration) forward-only rollback regression

## Outstanding

- T013 production-scale smoke: deferred to streaming follow-up spec.
- T024–T031: Phase 6 polish (source-grep guards, ruff, observability log, FR-011 health endpoint, PR description).
- T029 final live smoke: combined with T013 deferral above.
